# Match Registry Status

Match registry support exists in the repo but is not part of the core live flow today.
It should either be fully integrated into the main match path or explicitly treated as optional telemetry infrastructure.
