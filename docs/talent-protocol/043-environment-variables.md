# Environment Variables

This project relies on a broad env surface for wallets, contracts, and deployment behavior.
Consistency between local and Vercel env configuration matters more than most UI-only apps.
