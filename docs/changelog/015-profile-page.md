# Player Profile Page

The profile page aggregates wallet-linked data: match history, season points, owned cards, and attunement state.
All data is fetched server-side from Redis to avoid client-side wallet reads on sensitive routes.
