# Production Build Configuration

Output mode set to standalone for efficient containerised deployment.
Source maps are disabled in production to reduce bundle size.
Environment variable validation runs at build time to catch missing keys early.
