# Celo Network Integration

The app targets Celo mainnet exclusively.
Chain ID 42220 is enforced at the wallet connection layer with an automatic switch prompt.
Native CELO and ERC-20 CELO paths are handled uniformly via the GoldToken contract.
