# Webpack Custom Configuration

SVG imports are handled via @svgr/webpack for inline React component usage.
Large third-party modules are split into separate chunks to reduce initial JS payload.
Bundle analyser is available as an opt-in build flag for local profiling.
