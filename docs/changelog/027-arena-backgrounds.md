# Arena Backgrounds

Each character matchup renders a unique arena background during gameplay.
Backgrounds are paired by fighter combination and served as static assets.
Missing pairings fall back gracefully to the nearest available background.
