# Mobile Scaling Improvements

Refined the viewport scale transform on small screens to prevent content overflow.
Landscape lock is now enforced earlier in the render cycle, reducing layout flash on load.
