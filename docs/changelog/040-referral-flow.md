# Referral Flow

Players can generate a referral link tied to their wallet address.
New users who join via a referral link are attributed on the referrer's profile.
Referral codes are short-lived tokens validated server-side before attribution is written.
