"use client";
// RainbowKit CSS — loaded async so it doesn't block first paint.
// The wallet modal is never shown in MiniPay, so this can safely be deferred.
import "@rainbow-me/rainbowkit/styles.css";
export function RainbowKitStyles() { return null; }
